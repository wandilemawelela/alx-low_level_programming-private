#include "main.h"
#include <stdio.h>

/**
 * add -  adds two integers and returns the result.
 * @a: First integer
 * @b: Second integer
 * Return: Result of two added numbers
 *
 */

int add(int a, int b)
{
	return (a + b);
}
